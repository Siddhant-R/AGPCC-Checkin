<div class="container">
    <h2 class="section-subheading"> AGPCC Event Managament System</h2><br>

<% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
<% end %>

<div class="container">
    <% if @current_events.any? %>
      <h4>Ongoing Events</h4>
      <table class="table table-hover">
      <thead>
        <tr>
          <th>Event</th>
          <th>Venue</th>
          <th>Ending</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% @current_events.each do |event| %> 
            <tr>
              <td><%= event.title %></td>
              <td><%= event.venue %></td>
              <td><%= event.end_time.to_formatted_s(:short) %></td>
              <td><%= link_to 'Check In',controller: "check_in", action: "new", id: event.id %></td>
            </tr>
        <% end %>
      </tbody>
      </table>
    <% else %>
      <h4><b>No ongoing Events</b></h4>
      <br><br><br>
    <% end %>
</div>
<div class="container">
  <% if @current_events.any? %>
    <h4>Future Events</h4>
    <table class="table table-hover">
    <thead>
      <tr>
        <th>Event Title</th>
        <th>Venue</th>
        <th>From</th>
        <th>Till</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% @future_events.each do |event| %> 
          <tr>
            <td><%= event.title %></td>
            <td><%= event.venue %></td>
            <td><%= event.start_time.to_formatted_s(:short) %></td>
            <td><%= event.end_time.to_formatted_s(:short) %></td>
            <td><%= link_to 'R.S.V.P.',controller: "rsvp", action: "new", id: event.id %></td> 
          </tr>
      <% end %>
    </tbody>
    </table>
    <% end %>
</div>
    <h2 class="section-subheading"> Our Sponsors</h2><br>

<!-- SPONSORS -->
<div id="myCarousel" class="carousel slide" data-ride="carousel">
  <!-- Indicators -->
  <ol class="carousel-indicators">
    <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
    <li data-target="#myCarousel" data-slide-to="1"></li>
    <li data-target="#myCarousel" data-slide-to="2"></li>
    <li data-target="#myCarousel" data-slide-to="3"></li>
  </ol>

  <!-- Wrapper for slides -->
  <div class="carousel-inner" role="listbox">
    <div class="item active">
      <img src="http://www.agpcc.club/o/media/sponsors/campusview.jpg" alt="Chania">
    </div>

    <div class="item">
      <img src="http://www.agpcc.club/o/media/sponsors/ypa.jpg" alt="Chania">
    </div>

    <div class="item">
      <img src="http://www.agpcc.club/o/media/sponsors/classof86grad.jpg" alt="Flower">
    </div>

    <div class="item">
      <img src="http://www.agpcc.club/o/media/sponsors/janney.jpg" alt="Flower">
    </div>
  </div>

  <!-- Left and right controls -->
  <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
</div>
<br>
<br>
